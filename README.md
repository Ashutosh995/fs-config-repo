

# EC2 Filesystem Configuration

This repository contains configuration information

## 📂 Contents

  - Block device structure (`lsblk`)
- `fstab-backup`: A direct backup of the current `/etc/fstab` file on the instance.


### Run the Script

To inspect the filesystem configuration, run:

```bash
chmod +x fs-setup.sh
./fs-setup.sh
